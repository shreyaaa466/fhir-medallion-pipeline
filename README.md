# fhir-medallion-pipeline
FHIR API data ingestion pipeline using Microsoft Fabric — implements Medallion Architecture (Raw, Bronze, Silver, Gold) with SCD Type 2 versioning for Patient, Encounter, Observation, and Condition resources.
